<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SneakPeeks</title>
    <link href="./stylesheets/output.css" rel="stylesheet">
</head>
<body class="bg-black text-white min-h-screen relative overflow-x-hidden">
  <%- include("partials/alerts") %>
  <!-- Logo -->
  <div class="absolute top-5 left-6 text-xl md:text-3xl font-extrabold tracking-widest z-10">
    SNEAK<span class="text-red-500">PEEKS</span>
  </div>

  <!-- Main Container -->
  <div class="flex flex-col md:flex-row min-h-screen pt-24 md:pt-0">

    <div class="w-full md:w-1/2 flex items-center justify-center
                md:border-r border-white/10 px-6 py-10">

      <div class="w-full max-w-md">

        <h1 class="text-2xl md:text-4xl font-bold mb-2">Create Account</h1>
        <p class="text-gray-400 mb-8 text-sm md:text-base">
          Join &nbsp<span class="text-3xl font-bold">SNEAK<span class="text-red-500">PEEKS</span></span> &nbsp& Step Into Style
        </p>

        <form class="space-y-5" method="post" action="/users/register">

          <input
            type="text"
            name="name"
            placeholder="Full Name"
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
          />

          <input
            type="email"
            placeholder="Email"
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
            name="email"
          />

          <input
            type="number"
            placeholder="Mobile No."
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
            name="contact"
          />

          <input
            type="password"
            placeholder="Password"
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
            name="password"
          />

          <button
            class="w-full bg-white text-black font-bold py-3 rounded-lg
                   hover:bg-gray-200 active:scale-95 transition">
            Register
          </button>

        </form>

      </div>
    </div>

    <!-- LOGIN (RIGHT on desktop, BOTTOM on mobile) -->
    <div class="w-full md:w-1/2 flex items-center justify-center px-6 py-10">

      <div class="w-full max-w-md">

        <h1 class="text-2xl md:text-3xl font-bold mb-2">Welcome Back</h1>
        <p class="text-gray-400 mb-8 text-sm md:text-base">
          Login to continue your sneaker hunt
        </p>

        <form class="space-y-5" method="post" action="/users/login">

          <input
            type="email"
            placeholder="Email"
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
            name="email"
          />

          <input
            type="password"
            placeholder="Password"
            class="w-full bg-transparent border border-white/20 px-4 py-3 rounded-lg
                   focus:outline-none focus:border-white placeholder-gray-500"
            name="password"
          />

          <button
            class="w-full bg-white text-black font-bold py-3 rounded-lg
                   hover:bg-gray-200 active:scale-95 transition">
            Login
          </button>

        </form>
        <p class="text-xs text-gray-400 text-center mt-4">
          Are you an admin?
          <a href="/owners/admin/login" class="underline hover:text-white transition">
            Login here
          </a>
        </p>


      </div>
    </div>

  </div>

</body>
</html>