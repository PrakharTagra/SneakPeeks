<nav class="w-full bg-black border-b border-white/10 fixed top-0 z-50">
  <div class="max-w-7xl mx-auto px-5 md:px-10">

    <!-- Desktop Navbar -->
    <div class="flex items-center justify-between h-16">

      <!-- Left: Logo -->
      <div class="flex items-center gap-8">
        <a href="/" class="text-xl font-extrabold tracking-widest">
          SNEAK<span class="text-red-500">PEEKS</span>
        </a>

        <!-- Desktop Links -->
        <div class="hidden md:flex gap-6 text-sm text-gray-300">
          <a href="/shop" class="hover:text-white">Shop</a>
          <a href="/about" class="hover:text-white">About</a>
        </div>
      </div>

      <!-- Right: Icons -->
      <div class="flex items-center gap-5">

        <!-- Cart -->
        <a href="/cart" class="relative">
          🛒
          <span class="absolute -top-2 -right-2 bg-white text-black text-xs
                       font-bold px-1.5 rounded-full">
            <%=user.cart.length%>
          </span>
        </a>

        <!-- Profile / Login -->
        <a href="/users/profile" class="hidden md:block text-sm hover:text-gray-300">
          <div class="w-10 h-10 rounded-full overflow-hidden">
            <img
              src="data:image/png;base64,<%= user.profile.toString('base64') %>"
              alt="Profile Photo"
              class="w-full h-full object-cover"/>
          </div>
        </a>

        <!-- Mobile Menu Button -->
        <button class="md:hidden" onclick="toggleMenu()">
          ☰
        </button>
      </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="hidden md:hidden pb-6 pt-2 space-y-4">

      <input
        type="text"
        placeholder="Search sneakers..."
        class="w-full bg-black border border-white/20 px-4 py-2 rounded-lg
               text-sm placeholder-gray-500 focus:outline-none"
      />

      <a href="/shop" class="block text-gray-300 hover:text-white">Shop</a>
      <a href="/new" class="block text-gray-300 hover:text-white">New</a>
      <a href="/discounts" class="block text-gray-300 hover:text-white">Discounts</a>
      <a href="/about" class="block text-gray-300 hover:text-white">About</a>
      <a href="/profile" class="block text-gray-300 hover:text-white">Profile</a>
      <a href="/cart" class="block text-gray-300 hover:text-white">Cart</a>

    </div>

  </div>
</nav>

<script>
  function toggleMenu() {
    document.getElementById("mobileMenu").classList.toggle("hidden");
  }
</script>